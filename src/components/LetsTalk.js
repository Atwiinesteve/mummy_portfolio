// importing react.
import React from 'react';

// importing styles.
import "../styles/LetsTalk.css";

function LetsTalk() {

  return (
		<>
			<section id="lets--talk--area">
				<div className="lets--talk--header">
					<h2>Let's Talk</h2>
				</div>
				<div className="lets--talk--content">
					<p>
						We're open 7 days a week with access to psychology,
						psychotherapy and counselling with no waiting list. Easy booking,
						see a therapist the same day.
					</p>
				</div>
			</section>
		</>
	);

};

// exporting module.
export default LetsTalk;
